import { addtion, subtraction, multiplication, division, Action } from './Redux/Actions/types'


export const reducer = (actionObject: Action) => {
    switch (actionObject.type) {
        case addtion:
            return 
            break;
        case subtraction:
            break;
        case multiplication:
            break;
        case division:
            break;

    }
}